# Backup — homepage v1

Snapshot of the original `(web)/+page.svelte` and `+page.ts` taken when the v2
homepage was promoted to `/`. Restore by moving these two files back into
`src/routes/(web)/` (and deleting the current `+page.svelte`, `+page.server.ts`,
`data.ts`).

This folder lives outside `src/routes/` so SvelteKit ignores it.
