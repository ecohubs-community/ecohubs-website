# allow crawling everything by default
User-agent: *
Allow: /

# Keep crawlers out of server endpoints — these only return JSON/errors
# (form relays for contact, newsletter, Mautic and the waitlist) and have
# nothing to index.
Disallow: /api/

Sitemap: https://ecohubs.community/sitemap.xml
